version: 0.1.0
type: deployable-architecture
metadata:
  name: vibe-code-landing-zone
  displayName: "Vibe Code Landing Zone"
  description: >
    Stop configuring. Start feeling. This deployable architecture turns a
    single HTML document into a live static site on IBM Cloud Object Storage — in minutes.
  tags:
    - ibm-cloud
    - deployable-architecture
    - object-storage
    - static-website
    - cos
    - vibe-coding
    - html
inputs:
  - name: region
    type: string
    ui_hints:
      label: "Region"
      control: region
  - name: cos_instance_name
    type: string
    ui_hints:
      label: "COS Instance Name"
  - name: bucket_name
    type: string
    ui_hints:
      label: "Bucket Name"
  - name: index_html
    type: string
    ui_hints:
      label: "App HTML (Paste)"
      group: "App Content"
outputs:
  - name: vibe_url
    type: string
    key_output: true
  - name: vibe_bucket_url
    type: string
