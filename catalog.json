{
  "schema_version": "1.0.0",
  "name": "vibe-da-ibm-cloud",
  "version": "0.2.9",
  "type": "terraform",
  "provider": "ibm",
  "display_name": "Vibe IDE — Deployable Architecture",
  "description": "A deployable architecture that turns raw HTML into a live Vibe app hosted on IBM Cloud Object Storage.",
  "variables": [
    {
      "name": "vibe_code_b64",
      "type": "string",
      "description": "Paste your HTML here — it’s automatically encoded to base64 before deployment.",
      "ui:widget": "textarea",
      "ui:onChange": "value = btoa(unescape(encodeURIComponent(value)))"
    }
  ],
  "outputs": [
    {
      "name": "vibe_url",
      "type": "string",
      "description": "Public URL for your deployed Vibe app",
      "is_primary_output": true
    }
  ]
}
